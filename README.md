# simple-authenticated-PHP-web-shell
A simple and authenticated PHP command executor for securely running system commands via a web interface. This tool verifies a password before executing any commands.
